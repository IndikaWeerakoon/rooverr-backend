{
  "name": "roover-backend",
  "version": "1.0.0",
  "private": true,
  "description": "rooverr mono repo for all micro services",
  "main": "index.js",

  "workspaces": {
    "packages": [
      "services/*"
    ]
  },

  "author": "hasitha indika",
  "license": "F-CODE",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run --parallel clean",
    "release": "lerna publish",
    "kill": "kill-port --port 3000"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "rimraf": "^3.0.1"
  }
}
